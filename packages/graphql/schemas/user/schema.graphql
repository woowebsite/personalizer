scalar Date

type User {
  id: Int
  email: String
  name: String
  image: String
  created_at: Date
  updated_at: Date
  email_verified: Date
  role: Role
}

input UserWhere {
  roleId: Int
}

input UserInput {
  name: String
  email: String
  password: String
  image: String
}

type Query {
  user(id: Int): User 
  users(where: UserWhere, limit: Int, offset: Int): [User]
  loginUser(email: String!, password: String!): User
}

type Mutation {
  createUser(data: UserInput): User
}
