type ProductBaseTerm {
  id: Int
  term_taxonomy_id: Int
  order: Int
  ref_id: Int

  productBase: ProductBase
  termTaxonomy: TermTaxonomy
}

type Query {
  productBaseTerms(where: ProductBaseWhere): [ProductBaseTerm]
}
