type Companies {
  id: Int
  name: String
  users: [User]
}

input CompanyWhere {
  id: Int
}

input CompanyInput {
  name: String
}

type Query {
  getCompany(where: CompanyWhere, limit: Int, offset: Int): Companies
}

type Mutation {
  createCompany(data: CompanyInput): Company
}
