type Query {
  workflows(where: WorkflowWhere): Workflow
}

type Workflow {
  lanes: [Lane]
}

input WorkflowWhere {
  title: String
  taxonomies: [Int]
  user_id: Int
}

type Lane {
  id: Int
  title: String
  cards: [Job]
}
