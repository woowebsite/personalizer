# for ComboBox

```ts
renderComboBox = {};
```

Use `lableInValue` to get value as {value, name}

```ts
<ComboBoxEnum
  onChange={handleOnChange}
  type={ProductBaseVisibility}
  labelInValue
  {...rest}
/>
```
